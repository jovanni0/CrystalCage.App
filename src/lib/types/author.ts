export type Author = {
    id: string
    first_name: string
    last_name: string
}


type AuthorRequest = {
    first_name: string
    last_name: string
}


type AuthorResponse = {
    id: string
    first_name: string
    last_name: string
}